{% extends "admin/base_admin.html.twig" %}

{% block title %}Modification du Produit{% endblock %}

{% block body %}

<main id="main">
    <section class="adminForm">

        <div class="form-cate">
    
            <div class="retour">
                <a href="{{ path('admin_product_index') }}" class=""><i class="fa-solid fa-arrow-left"></i> Retour à la liste</a>
            </div>
       
            <div class="col-12">
                <div class="col-12">
                    <h1 class="h1catadmin">Modifier le produit</h1>
                </div>
                <div id="cate-form">
                      {{ include('admin/product/_form.html.twig', {'button_label': 'Update'}) }}
                      {{ include('admin/product/_delete_form.html.twig') }}
                </div>              
            </div>
        </div>
    </section>
</main> 

{% endblock %}

